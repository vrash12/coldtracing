/*
 * Shared browser bootstrap module.
 *
 * Keep application-wide JavaScript initialization here as integrations are
 * added. The module currently has no global dependencies, but it remains the
 * stable import target used by resources/js/app.js.
 */
